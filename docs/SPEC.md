# Vibe Engine 規格書

> 從 Vibe Coding 進化到 Vibe Engineering 的基礎設施

---

## 一、願景與定位

### 核心理念

**Vibe Engineering** = 把 AI 當作工程流程的一級成員

| 面向 | Vibe Coding | Vibe Engineering |
|------|-------------|------------------|
| 本質 | 描述想法，AI 生成程式碼 | 系統化的 AI 工程工作流 |
| 特性 | 隨意、實驗性 | 有規範、可重現、有監控 |
| 品質 | 快速原型 | **生產級** (Production-aware) |
| 角色 | 使用者 | **AI 團隊的管理者** |

### Vibe Engine 的目標

1. **並行運行多個 agents** - 同時處理多個問題
2. **系統化的工作流** - 共享的 prompt 庫、agent 配置
3. **可重現的結果** - 結構化的 AI-agent 互動，有 logs 和監控
4. **生產級品質** - AI 貢獻必須可靠、安全、可維護

### 與其他專案的定位差異

| 面向 | oh-my-claudecode | OpenClaw | Vibe Engine |
|------|------------------|----------|-------------|
| 形式 | Claude Code Plugin | 獨立系統 | Claude Code Plugin |
| 重點 | 多代理編排 | 多渠道通訊 | **Vibe Engineering 工作流** |
| 複雜度 | 功能豐富 | 完整平台 | **輕量但可擴展** |
| 哲學 | 最大化功能 | 本地優先 | **工程化 AI 協作** |

---

## 二、核心架構

### 星型代理架構

```
SubAgent ←→ Main Agent ←→ SubAgent
              ↑
              │
            User (工程師 = AI 團隊管理者)
```

**通訊規則**：
- ✅ User ←→ Main Agent ←→ SubAgent
- ❌ SubAgent ←→ SubAgent（禁止直接通訊）

### Main Agent 的角色

> "Router, Not Executor"

```yaml
main_agent:
  allowed:
    - analyze_request      # 分析請求意圖
    - decompose_task       # 分解任務
    - select_agents        # 選擇合適的 Agent
    - delegate_work        # 委派工作
    - synthesize_results   # 彙整結果

  forbidden:
    - direct_file_editing  # 不直接編輯文件
    - direct_code_writing  # 不直接寫代碼

  performance:
    max_thinking_time: 30s
    target_delegation_time: 5s
    prefer_parallel: true
```

### 整體架構圖

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          VIBE ENGINE                                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │                      🎯 MAIN AGENT (中央協調者)                    │ │
│  │  • 任務理解與分解  • SubAgent 調度  • 結果彙整  • 上下文管理       │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                                    │                                    │
│                    ┌───────────────┼───────────────┐                   │
│                    ▼               ▼               ▼                   │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                      🤖 SUB-AGENTS                               │  │
│  │  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐       │  │
│  │  │ Architect │ │ Developer │ │  Tester   │ │ Reviewer  │  ...  │  │
│  │  └─────┬─────┘ └─────┬─────┘ └─────┬─────┘ └─────┬─────┘       │  │
│  │        └─────────────┴──────┬──────┴─────────────┘              │  │
│  │                             ▼                                   │  │
│  │                    ┌─────────────────┐                          │  │
│  │                    │   ⚡ SKILLS     │  ← SubAgents 調用技能     │  │
│  │                    └─────────────────┘                          │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                        │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                       🪝 HOOKS                                   │  │
│  │   PreTask ─→ PreAgent ─→ [Agent執行] ─→ PostAgent ─→ PostTask   │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                        │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 三、四大核心模組

### 1. Plugin (模組容器)

```
my-plugin/
├── .claude-plugin/
│   └── plugin.json          # 必需：manifest
├── agents/                   # SubAgent 定義
├── skills/                   # Skill 定義
├── hooks/
│   └── hooks.json           # Hook 配置
└── scripts/                  # 腳本
```

### 2. Agent (執行者)

```yaml
---
name: code-reviewer
description: 專家程式碼審查員。代碼更改後主動進行審查。
tools: Read, Grep, Glob, Bash
model: sonnet
---

你是資深程式碼審查員...
```

**關鍵規則**：
- SubAgents 只能被 Main Agent 調用
- SubAgents 之間不能直接通訊
- 每個 Agent 有明確的職責邊界

### 3. Hook (攔截點)

| 事件 | 觸發時點 | 用途 |
|------|---------|------|
| `SessionStart` | 會話開始 | 初始化、載入上下文 |
| `UserPromptSubmit` | 用戶提交 | 關鍵詞檢測、模式啟動 |
| `PreToolUse` | 工具使用前 | 權限驗證、輸入檢查 |
| `PostToolUse` | 工具使用後 | 結果驗證、錯誤恢復 |
| `Stop` | Claude 停止前 | 驗證完成、強制繼續 |

### 4. Skill (能力)

```
skill-name/
├── SKILL.md                  # 主文件（必需）
├── reference.md              # 參考資料（可選）
└── scripts/                  # 腳本（可選）
```

---

## 四、模組優先級

### P0 (必須有) - 核心功能

| 模組 | 內容 |
|------|------|
| **core/orchestrator** | 基礎編排、任務分解、代理調度 |
| **core/config** | 配置管理、優先級覆蓋 |
| **agents/builtin** | 5 個核心代理：architect, developer, reviewer, tester, explorer |
| **skills/execution** | 基礎執行技能：default, plan, orchestrate |
| **hooks/lifecycle** | SessionStart, UserPromptSubmit, Stop |
| **protocols/communication** | 星型通訊協議 |

### P1 (應該有) - 增強功能

| 模組 | 內容 |
|------|------|
| **core/state** | 狀態管理、Checkpoint、中斷恢復 |
| **agents/builtin** | 額外代理：planner, debugger, documenter, researcher, security |
| **skills/enhancement** | parallel, git-flow, security |
| **skills/guarantee** | ralph, verify-build, verify-tests |
| **hooks/validation** | 驗證鉤子 |
| **protocols/verification** | BUILD, TEST, LINT 驗證協議 |

### P2 (可以有) - 進階功能

- core/gateway - 控制平面
- workflows/runtime - 工作流運行時
- tools/mcp - MCP 整合

---

## 五、內建代理規劃

| 代理 | 職責 | 複雜度 | 模型 | 優先級 |
|------|------|--------|------|--------|
| `architect` | 架構設計、技術決策 | HIGH | Opus | P0 |
| `developer` | 程式碼實現 | MEDIUM | Sonnet | P0 |
| `reviewer` | 程式碼審查 | MEDIUM | Sonnet | P0 |
| `tester` | 測試撰寫、執行 | MEDIUM | Sonnet | P0 |
| `explorer` | 程式碼搜尋、分析 | LOW | Haiku | P0 |
| `planner` | 任務規劃、拆解 | HIGH | Opus | P1 |
| `debugger` | 除錯、問題診斷 | MEDIUM | Sonnet | P1 |
| `documenter` | 文檔撰寫 | LOW | Haiku | P1 |
| `researcher` | 文檔研究、資料蒐集 | MEDIUM | Sonnet | P1 |
| `security` | 安全審查 | HIGH | Opus | P1 |

---

## 六、Skills 雙維度架構

結合 oh-my-claudecode（功能層級）和 OpenClaw（來源層級）：

```
┌─────────────────────────────────────────────────────────────┐
│                     Skills 雙維度架構                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  維度一：功能層級                                            │
│  ┌─────────────┬─────────────┬─────────────┐               │
│  │  Execution  │ Enhancement │  Guarantee  │               │
│  │   執行層    │    增強層   │    保證層    │               │
│  └─────────────┴─────────────┴─────────────┘               │
│                                                             │
│  維度二：來源層級                                            │
│  ┌─────────────┬─────────────┬─────────────┐               │
│  │   Bundled   │   Managed   │  Workspace  │               │
│  │    預裝     │    策展     │   用戶定義   │               │
│  └─────────────┴─────────────┴─────────────┘               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 執行層技能

| 技能 | 功能 |
|------|------|
| `default` | 標準構建流程 |
| `plan` | 只規劃不執行 |
| `orchestrate` | 多代理協調 |

### 增強層技能

| 技能 | 功能 |
|------|------|
| `parallel` | 並行執行多任務 |
| `git-flow` | Git 工作流管理 |
| `security` | 安全檢查增強 |

### 保證層技能

| 技能 | 功能 |
|------|------|
| `ralph` | 持續執行直到驗證完成 |
| `verify-build` | 確保構建通過 |
| `verify-tests` | 確保測試通過 |

---

## 七、驗證協議

| 協議 | 功能 |
|------|------|
| `BUILD` | 確保構建通過 |
| `TEST` | 確保測試通過 |
| `LINT` | 確保 lint 通過 |
| `FUNCTIONALITY` | 確保功能正確 |
| `ARCHITECT` | Opus 級架構審查 |

---

## 八、載入優先級

```
載入順序（後者可覆蓋前者）:

1. vibe-engine/core        ← 內建基礎
2. ~/.claude/plugins/*     ← 全域 plugins
3. ./.claude/plugins/*     ← 專案 plugins
4. ./CLAUDE.md rules       ← 專案規則覆蓋
```

---

## 參考資源

- **深度研究**：[DEEP-RESEARCH.md](DEEP-RESEARCH.md) - 完整的研究背景和解決方案
- **oh-my-claudecode**：https://github.com/Yeachan-Heo/oh-my-claudecode
- **OpenClaw**：https://github.com/openclaw/openclaw
